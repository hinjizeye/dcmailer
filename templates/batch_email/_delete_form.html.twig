<form method="post" action="{{ path('batch_email_delete', {'id': batch_email.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ batch_email.id) }}">
    <button class="btn">Delete</button>
</form>

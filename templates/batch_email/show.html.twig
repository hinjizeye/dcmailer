{% extends 'dashboard.html.twig' %}

{% block title %}View Email
{% endblock %}

{% block body %}
	<div id="layoutSidenav_content">
		<main>
			<div
				class="container-fluid">
				{# <h4 class="mt-4">ADD EMAIL</h4> #}
				<ol class="breadcrumb mb-4">
					<li class="breadcrumb-item">
						<a href="index.html">Dashboard</a>
					</li>
					<li class="breadcrumb-item active">Viewing Email</li>
				</ol>
				<div class="card mb-4">
					<div class="card-body">
						<table class="table">
							<tbody>
								<tr>
									<th>Subject</th>
									<td>{{ batch_email.subject | raw}}</td>
								</tr>
								<tr>
									<th>Message</th>
									<td>{{ batch_email.message | raw }}</td>
								</tr>
							</tbody>
						</table>

						{# <a href="{{ path('batch_email_index') }}">back to list</a>

						<a href="{{ path('batch_email_edit', {'id': batch_email.id}) }}">edit</a>

						{{ include('batch_email/_delete_form.html.twig') }} #}
					</div>
				</div>
			</div>
		</main>
	{% endblock %}
